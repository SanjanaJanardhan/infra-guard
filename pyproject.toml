[project]
name = "infra-guard"
version = "0.1.0"
description = "MCP server that scans Terraform for real security misconfigurations using Checkov"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=1.3.0",
    "checkov>=3.3.13",
    "fastapi>=0.141.1",
    "mcp[cli]>=2.0.0",
    "python-dotenv>=1.2.3",
    "uvicorn[standard]>=0.52.4",
]
