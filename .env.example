# Copy to .env and fill in your own key to enable "Explain & Fix" locally.
# Get a key at https://console.anthropic.com/ — never commit the real value,
# and never set this on the public Railway deployment.
ANTHROPIC_API_KEY=
